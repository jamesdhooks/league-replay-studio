# League Replay Studio - Python Dependencies
# Core
fastapi==0.115.0
uvicorn[standard]==0.34.0
pydantic==2.10.0

# Desktop window
pywebview==5.3.0

# WebSocket
websockets==14.1

# Utilities
python-multipart==0.0.18

# iRacing SDK — shared memory telemetry + Broadcasting API
# pyirsdk: Python 3 implementation of iRacing SDK
# https://github.com/kutu/pyirsdk
pyirsdk==1.3.0

# Screenshot capture (iRacing window embed)
mss==9.0.2
Pillow==10.4.0

# High-performance screen capture (DXGI Desktop Duplication API)
# Optional: falls back to PrintWindow if not installed
dxcam==0.0.5
numpy==1.26.4
opencv-python-headless==4.10.0.84  # required by dxcam for color conversion

# YouTube Data API integration
httpx==0.27.2

# Overlay template rendering
jinja2==3.1.4

# Testing
pytest==8.3.0
pytest-asyncio==0.24.0
